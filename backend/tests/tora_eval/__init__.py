"""TORA eval harness — golden-question regression tests.

Run with:
    cd backend && python -m pytest tests/tora_eval/ -v

Or run the harness directly to get a scored report without pytest:
    cd backend && python -m tests.tora_eval.runner
"""
