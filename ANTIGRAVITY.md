# ANTIGRAVITY: Instructions for the Coding Assistant

This file provides project-specific context for the **Antigravity** AI assistant to ensure high-fidelity code generation and system reasoning within the Spendsy platform.

## 🛠️ Environment Context
- **OS**: Windows 11 (PowerShell is the default shell).
- **Orchestration**: Prefer `.\run-local.ps1` for local development.
- **Python**: 3.11+. Standardize on `python` (not `python3`).
- **Database**: PostgreSQL 15 via PgBouncer (Port 6432).

## 🤖 TORA Intelligence 2.0
- **Model**: Gemma 4 E2B (`gemma4:e2b`).
- **Memory**: Persistent Obsidian Vault at `backend/vaults/user_{id}/`.
- **Reasoning**: Always use the 4-stage entity resolver logic (**Exact → Synonym → Fuzzy → Fallback**).
- **Accuracy**: Absolute financial integrity via `Decimal` type is non-negotiable.

## 🕸️ Graphify (Codebase Memory)
- **Knowledge Graph**: Located in `graphify-out/`.
- **Task**: Before large refactors, read `graphify-out/GRAPH_REPORT.md` to identify high-centrality "God Nodes" (e.g., `finance-service/app/main.py`).
- **Maintenance**: After modifying multiple files, run `graphify update .` to keep AST indices synchronized.

## ⚖️ Standards
- **Inter-service**: Always use `tenacity` retries and timeouts for HTTP calls.
- **UI**: Modern, premium dark mode using Vanilla CSS or Tailwind (per user request).
- **Testing**: Regression baseline is the 1200-query stress harness (`backend/tests/tora_eval/stress/`).

---
*Generated by Antigravity for Antigravity.*
